import 'package:flutter/material.dart';

class SettingsScreen extends StatelessWidget { final bool dark; final ValueChanged<bool> onDark; final VoidCallback clearCompleted, reset; const SettingsScreen({super.key,required this.dark,required this.onDark,required this.clearCompleted,required this.reset}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Settings')),body:ListView(padding:const EdgeInsets.all(22),children:[const Text('PREFERENCES',style:TextStyle(fontSize:11,fontWeight:FontWeight.w800,letterSpacing:1.5)),SwitchListTile(contentPadding:EdgeInsets.zero,title:const Text('Dark appearance'),subtitle:const Text('A dedicated deep-space productivity theme'),value:dark,onChanged:onDark),const Divider(height:28),ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.cleaning_services_outlined),title:const Text('Clear completed tasks'),onTap:clearCompleted),ListTile(contentPadding:EdgeInsets.zero,leading:const Icon(Icons.restart_alt_rounded),title:const Text('Reset all data'),onTap:reset),const SizedBox(height:80),Center(child:Text('Developed by Maha',style:TextStyle(color:Theme.of(context).colorScheme.onSurfaceVariant,fontSize:12))) ])); }
